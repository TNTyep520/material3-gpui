[workspace]
members = ["catalog"]

[package]
name = "material3-gpui"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
description = "Material Design 3 component library for gpui"
repository = "https://github.com/TNTyep520/md3-gpui"
keywords = ["gui", "ui", "material-design", "gpui", "components"]
categories = ["gui"]
exclude = ["catalog"]

# 源码目录由 src 命名为 crates
[lib]
path = "crates/material3_gpui.rs"

[features]
dynamic-color = ["dep:mcu-hct", "dep:mcu-palettes", "dep:mcu-dynamiccolor"]

[dependencies]
gpui = "0.2.2"
anyhow = "1"
mcu-hct = { version = "0.2.1", optional = true }
mcu-palettes = { version = "0.2.0", optional = true }
mcu-dynamiccolor = { version = "0.2.4", optional = true }

[lints.clippy]
redundant_clone = "warn"

[profile.dev]
panic = "abort"
debug = "limited"
split-debuginfo = "unpacked"
incremental = true

[profile.dev.package]
taffy = { opt-level = 3 }

[profile.release]
opt-level = "z"
strip = true
lto = true
codegen-units = 1
panic = "abort"
debug = false
debug-assertions = false
overflow-checks = false
incremental = false
rpath = false

[profile.release-fast]
inherits = "release"
opt-level = "z"
strip = true
codegen-units = 1
debug = false
debug-assertions = false
overflow-checks = false
incremental = false
